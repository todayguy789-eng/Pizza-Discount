<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Pizza Discount Reminder</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      
      /* Background image of your choice */
      background-image: url('https://th.bing.com/th/id/OIP.kHRFCs65vOOR8agun1AHLAHaF7?w=222&h=180&c=7&r=0&o=7&dpr=1.3&pid=1.7&rm=3');
      background-size: cover; /* Make sure the image covers the entire background */
      background-position: center; /* Center the image */
      background-repeat: no-repeat;
      
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      margin: 0;
      padding: 20px;
      
      /* Optional: add overlay for readability */
      /* Uncomment if needed:
      background-color: rgba(255,255,255,0.5);
      background-blend-mode: overlay;
      */
    }
    h1 {
      color: green;
      text-shadow: 1px 1px 4px rgba(0,0,0,0.5);
    }
    p {
      font-size: 1.2em;
      margin: 10px 0;
    }
    .message-box {
      background-color: rgba(255,255,255,0.8);
      border: 2px solid #ff8c00;
      border-radius: 10px;
      padding: 30px;
      max-width: 600px;
      margin-top: 30px;
      color: green;
      text-align: center;
    }
  </style>
</head>
<body>
  <h1 style="color: green;">Thank You for Visiting!</h1>
  <div class="message-box">
    <p>We are sorry, but this discount code has already been used.</p>
    <p>Download the <strong>Pizza Hut App</strong> to receive the most up-to-date discounts and rewards!</p>
  </div>

  <!-- Firebase SDK -->
  <script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-app-compat.js"></script>
  <script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-database-compat.js"></script>

  <script>
    // Your Firebase configuration
    const firebaseConfig = {
      apiKey: "AIzaSyAnc3K9nfdcoZ90PuwX2V0l7tKDy9VoOCg",
      authDomain: "pizza-discount.firebaseapp.com",
      databaseURL: "https://pizza-discount-default-rtdb.firebaseio.com",
      projectId: "pizza-discount",
      storageBucket: "pizza-discount.firebasestorage.app",
      messagingSenderId: "978585513000",
      appId: "1:978585513000:web:dd694806f2c19e8c99591d",
      measurementId: "G-X6CZYWCXC1"
    };

    // Initialize Firebase
    firebase.initializeApp(firebaseConfig);
    const database = firebase.database();

    const countRef = database.ref('visitorCount');

    // Increment visitor count (optional)
    countRef.transaction((currentCount) => {
      if (currentCount === null) {
        return 1; // First visitor
      } else {
        return currentCount + 1; // Increment count
      }
    }, (error, committed, snapshot) => {
      if (error) {
        console.error("Transaction failed: ", error);
      } else if (!committed) {
        console.log("Transaction not committed");
      } else {
        // Optional: display count somewhere
        // document.getElementById('visitorCount').textContent = snapshot.val();
      }
    });
  </script>
</body>
</html>
